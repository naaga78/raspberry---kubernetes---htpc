apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  namespace: "media"
  name: "hdd-ssd"
spec:
  storageClassName: "manual"
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: "10Gi"
